#include<stdio.h>
int main()
{
	int i=100,j=0,ge,shi,bai;
	while(i<1000)
	{
		ge=i%10;
		shi=i/10%10;
		bai=i/100;
		if(ge==shi||ge==bai||shi==bai)
		{
			printf("%d\t",i);
			j++;
		}
		i++;
	}
	printf("\n符合要求的有%d个。\n",j);
	return 0;
}
